## 🚀 开源项目二开与同步更新流程

### 1. Fork 项目

* 在 GitHub 上 **Fork** 一份该开源项目到你自己的仓库（比如 `yourname/project`）。
* 这样你就有自己的独立远程仓库（origin）。

### 2. 克隆到本地

```bash
git clone git@github.com:yourname/project.git
cd project
```

### 3. 添加上游仓库（upstream）

把原始开源项目作为上游：

```bash
git remote add upstream git@github.com:original/project.git
git remote -v
# 确认有 origin 和 upstream 两个远程仓库
```

### 4. 创建自己的开发分支

不要直接在 `main`/`master` 上改动，建议开新分支：

```bash
git checkout -b feature/my-custom-dev
```

你所有的二开代码都放在这个分支。

### 5. 定期同步上游更新

当原项目更新时：

```bash
git fetch upstream
git checkout main
git merge upstream/main   # 或者 git rebase upstream/main
git push origin main
```

这样你的 `main` 会保持和上游一致。

然后把更新合并到你二开的分支：

```bash
git checkout feature/my-custom-dev
git merge main
# 解决冲突
git push origin feature/my-custom-dev
```

### 6. 管理二开代码与上游差异

有几种常见策略：

* **方式 A（推荐）**：保持 `main` 与上游一致，你所有改动放在 `feature/*` 分支。
* **方式 B**：维护一个 `your-main` 分支专门用于二开，然后定期 `rebase` 到 `upstream/main`。
* **方式 C**：使用 Git 的 `patch` 或 `cherry-pick`，只迁移你需要的改动。

### 7. 更新依赖 & 测试

每次同步上游后，注意：

* 跑 `git diff` 看上游改了哪些文件。
* 更新依赖（`requirements.txt`, `package.json`, `go.mod`, `pom.xml` 等）。
* 跑项目的测试，确认没有被上游改动破坏。

---

## 🔧 实战小技巧

* 用 `git rebase` 而不是 `merge`，可以保持历史干净。
* 如果二开内容很大，可以考虑做成一个 **插件/扩展**，减少和上游的耦合。
* 如果有通用的优化，最好尝试 **PR 回上游**，这样以后维护成本会低很多。

---
